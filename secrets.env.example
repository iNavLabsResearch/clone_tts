# Copy to secrets.env (gitignored) and fill in. Or export these in your shell / Kaggle Secrets.
# Do NOT commit real tokens.

# Hugging Face token with ACCEPTED access to ARTPARK-IISc/Vaani (gated) and write access
# for pushing milli_guj_dataset_artpark.
HF_TOKEN=

# Weights & Biases API key (optional; training falls back to no logging if unset).
WANDB_API_KEY=
